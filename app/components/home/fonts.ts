import { Schibsted_Grotesk } from 'next/font/google';

/** A compact, heavy grotesque in the spirit of the logo's wordmark; used for headlines and text alike. */
export const schibsted = Schibsted_Grotesk({
  subsets: ['latin'],
  variable: '--font-schibsted',
  display: 'swap',
});
