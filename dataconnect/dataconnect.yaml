specVersion: "v1beta"
serviceId: "helixcard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "helixcard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
